要查看实验结果，直接运行 lab2.py 文件即可，整个过程大约需要 5 分钟。程序运行时，会实时输出一些文字信息，比如以 “Finish ...” 开头的语句，这些语句用于提示当前的运行进度；同时也会输出一系列数字，这些数字代表的是匹配到的特征点数量。

实验所需的输入图像存放于当前文件夹下的 img 文件夹内，而程序运行完成后生成的输出图像，则会自动保存到同目录下的 output 文件夹中。